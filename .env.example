BOT_TOKEN=
CHAT_ID=

# Telegram user IDs allowed to issue commands, comma-separated.
# Optional: defaults to CHAT_ID, which in a private chat is your own user ID.
# Required if CHAT_ID is negative (a group or channel), since a group ID is not
# a user ID and defaulting would accept commands from every member.
ALLOWED_USER_IDS=
